import React from 'react';
import { BaseComponent } from '../../components';
import styles from './Rate.scss';

export default class Rate extends BaseComponent {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <div className={styles.rateCss}>
        aa
      </div>
    );
  }
}
